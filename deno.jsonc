{
  "tasks": {
    "dev": "deno task vite",
    "build": "deno task vite build",
    "preview": "deno task vite preview",
    "serve": "deno run --allow-net --allow-read https://deno.land/std@0.157.0/http/file_server.ts build",
    "vite": "deno run -A --node-modules-dir npm:vite"
  }
}
